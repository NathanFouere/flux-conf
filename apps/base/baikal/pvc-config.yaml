apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: baikal-config
  labels:
    app.kubernetes.io/name: baikal
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 200Mi
