apiVersion: v1
kind: Service
metadata:
  name: cleanuparr
  namespace: media
  labels:
    app.kubernetes.io/name: cleanuparr
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: 11011
      protocol: TCP
  selector:
    app.kubernetes.io/name: cleanuparr
