apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: paseo-home
  labels:
    app.kubernetes.io/name: paseo
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: nfs
  resources:
    requests:
      storage: 500Mi
